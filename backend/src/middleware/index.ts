/**
 * Middleware Index
 * ================
 * Re-exports all middleware so routes can import them from one place.
 *
 * Example:
 *   import { verifyJWT } from "../middleware";
 */

export { verifyJWT } from "./verifyJWT";
